# Keep the build context small. The fallback (source) build only needs
# Cargo.toml, Cargo.lock, crates/, and vendor/, so everything below is safe to
# exclude.
.git
.github
.gitlab
.claude
.cargo
target
dist
fonts
docs
deploy
.env
.env.*
.DS_Store
